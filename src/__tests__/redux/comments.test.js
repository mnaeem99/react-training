import Comments from "../../redux/comments";


describe("Reducer",()=>{
    test("should return default state when loading",()=>{
        //expect(Comments(defaultState)).toEqual(defaultState);
    });
})